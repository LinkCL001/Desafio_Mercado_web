{{!// 4. Crear un parcial para la sección principal en donde se renderice un helper con el
// mensajedebienvenidayserealiceunaiteraciónpararenderizarunparcial“producto”
// pasándole como argumento el nombre de cada producto.}}
{{> Bienvenido}}

{{#each productos}}
    {{>Producto nombreProducto=this}}
{{/each}}

<script>
    let ProductoSeleccionado = new Set();

    const agregarCarro = (id) => {
        ProductoSeleccionado.add(id);
    }

    const carro = document.querySelector('#carroIcono')
    carro.addEventListener('click', () => {
        console.log(ProductoSeleccionado)
        const modal = document.querySelector('.modal-body');
        modal.innerHTML = '';
        ProductoSeleccionado.forEach(producto => {
        modal.innerHTML += `<img src="/img/${producto}.png" alt="${producto}">`    
        })        
    })
</script>